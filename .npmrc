# npm only *warns* about an unsupported "engines" range by default. This turns it into a
# hard failure, so an old Node gives a readable message instead of a stack trace out of the
# CommonJS loader when it tries to require() an ESM-only Vite.
engine-strict=true
